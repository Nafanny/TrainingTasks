package com.nafanny.tasks.primitive;

public class TypeBoolean {

    // Начнём с объявления переменных и их инициализации.
    boolean bool;   // При объявлении переменной по умолчанию она объявлена как (false - лож).
    boolean bool1 = true;   // Тут мы явно указываем на то что переменная bool1 хранит значение true.
}
