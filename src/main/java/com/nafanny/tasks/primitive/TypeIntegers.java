package com.nafanny.tasks.primitive;

public class TypeIntegers {

    // Начнём с объявления переменных и их инициализации.
    int n;      // Объявляем переменную n (то-есть выделяем облость в памяти) но не инициализируем её.
    int n1 = 1; // Объявляем переменную n1 и инициализируем её (выделяем облость в памяти и помещаем в неё значение 1).
    
}
